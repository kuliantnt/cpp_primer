/*
#include <iostream>
#include <cmath>
#include <iomanip>
//
// Created by lianlian on 17-1-9.
//

int main(int argc, char*argv[]) {
    std::cout
            << std::setprecision(10)
            << std::setw(20)
            << std::left
            << "default format: "
            << std::setw(40)
            << std::right
            << 100 * sqrt(2.0) << "\n"


            << std::setw(20)
            << std::left
            << "scientific: "
            << std::setw(40)
            << std::uppercase
            << std::right
            << std::scientific << 100 * sqrt(2.0) << "\n"

            << std::setw(20)
            << std::left
            << "fixed decimal: "
            << std::setw(40)
            << std::right
            << std::fixed << 100 * sqrt(2.0) << '\n'

            << std::setw(20)
            << std::left
            << "hex decimal: "
            << std::setw(40)
            << std::right
            << std::hexfloat << 100 * sqrt(2.0) << '\n'

            << std::setw(20)
            << std::left
            << "using defaults: " << std::defaultfloat
            << std::setw(40)
            << std::right
            << 100 * sqrt(2.0) << "\n\n";
}
*/
